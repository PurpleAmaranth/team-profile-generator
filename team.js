class Team {
    constructor(name) {
        this.name = name;
        this.members = 0;
    }
}

generateMemberCard(this.name) {

}

printMembers() {
    this.member.forEach(member +> {
        if (member.name)
    })
}

welcome() {
    console.log(`Welcome to the team ${this.name}`)
}
